pre-commit:
  commands:
    prettier:
      glob: '**/*.{js,jsx,ts,tsx,json,md,yml,yaml}'
      run: pnpm format --write {staged_files} && git add {staged_files}

pre-push:
  commands:
    build:
      run: pnpm build
    test:
      run: pnpm test
